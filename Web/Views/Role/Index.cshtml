@using Web.Data;

@model IEnumerable<Role>

@{
    ViewData["Title"] = "Index";
}

<h1>Roles</h1>

<a class="btn btn-primary" asp-controller="Role" asp-action="Create">Create Role</a>
<hr />
<table class="table table-striped">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Permissions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model)
        {
            <tr>
                <td>@role.Id</td>
                <td>@role.Name</td>
                <td>@string.Join(",", role.RolePermissions.Select(s => s.Permission.Name))</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@role.Id" class="btn btn-primary">编辑</a>
                    <a asp-action="Delete" asp-route-id="@role.Id" class="btn btn-danger">删除</a>
                </td>
            </tr>
        }
    </tbody>
</table>

